============
Installation
============

At the command line::

    $ easy_install general-log-parser

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv general-log-parser
    $ pip install general-log-parser
